const intialState = {
  name: null,
  private: null,
  default: null,
  members: [],
  messages: [],
};

const channelReducer = (state = intialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default channelReducer;
